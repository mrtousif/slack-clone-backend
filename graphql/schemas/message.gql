# const { gql } = require('apollo-server-express');

type Message {
    id: ID!
    text: String!
    user: User!
    channel: Channel!
}

type Query {
    hi: String!
}

type Mutation {
    createMessage(text: String!): Message!

    deleteMessage(id: ID!): Message
}
